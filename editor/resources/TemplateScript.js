import * as banana from '../../dist/banana.js';

export class NameOftheScript extends banana.ScriptableEntity {
    
    // Use this function for initialization.
    onCreate() {
        
    }

    // This function is called once per frame.
    onUpdate(deltaTime) {
        
    }
}